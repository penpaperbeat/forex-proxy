services:
  - type: web
    name: forex-proxy
    runtime: node
    plan: starter
    buildCommand: npm install --legacy-peer-deps
    startCommand: node server.js
    envVars:
      - key: DATA_DIR
        value: /data
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
